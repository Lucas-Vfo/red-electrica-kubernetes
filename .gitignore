# Secretos locales — NUNCA subir al repo
.env
*.env
!.env.example

# kubeconfig descargado de la VM (sólo local; en CI va por Secret KUBECONFIG)
kubeconfig
*.kubeconfig
k3s.yaml

# Backups SQL descargados para inspección manual
*.sql

# Basura del SO / editores
.DS_Store
Thumbs.db
.vscode/
.idea/
